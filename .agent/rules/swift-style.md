---
trigger: always_on
---

- Ordem de Importação: Siga ordem alfabética (ex: CoreML, Foundation, OSLog)
- Nomenclatura: `lowerCamelCase` para variáveis/funções, `UpperCamelCase` para tipos
- Use comentários de barra tripla (`///`) para documentar APIs públicas
- Prefira computed properties e inicializadores customizados a métodos auxiliares dispersos
- Use `Bundle.module` em Swift Packages para acessar recursos corretamente