# Workflow Router: Ship App

Canonical source: `../../macos-development/workflows/ship-app.md`

Use the canonical workflow above for implementation steps.

Repository verification override:

- Prefer `make build-test` during iteration.
- Use `make build-test` and `make lint` as hard gates for Medium/High risk changes.
