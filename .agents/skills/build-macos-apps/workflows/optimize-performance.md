# Workflow Router: Optimize Performance

Canonical source: `../../macos-development/workflows/optimize-performance.md`

Use the canonical workflow above for implementation steps.

Repository verification override:

- Prefer `make build-agent` and `make test-agent` during iteration.
- Use `make build`, `make test`, and `make lint` as hard gates for Medium/High risk changes.
